<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
  <!-- 讀取 application*.properties 的設定 -->
  <springProperty scope="context" name="LOG_PATH" source="logging.file.path" defaultValue="logs"/>
  <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="app"/>

  <!-- Pattern：INFO 簡潔（不含 MDC） -->
  <property name="LOG_PATTERN_INFO"
            value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{24} - %msg%n"/>

  <!-- Pattern：DEBUG 詳細（含 MDC: requestId, userId） -->
  <property name="LOG_PATTERN_DEBUG_MDC"
            value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{24} - %msg rid=%X{requestId} uid=%X{userId}%n"/>

  <!-- === Console（用 Profile 切換是否顯示 MDC） === -->

  <!-- dev：Console 顯示含 MDC 的詳細版 -->
  <springProfile name="dev">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder><pattern>${LOG_PATTERN_DEBUG_MDC}</pattern></encoder>
    </appender>
  </springProfile>

  <!-- 非 dev（如 prod）：Console 用精簡版 -->
  <springProfile name="!dev">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder><pattern>${LOG_PATTERN_INFO}</pattern></encoder>
    </appender>
  </springProfile>

  <!-- === 檔案輸出 === -->

  <!-- 一般檔案：INFO 以上（精簡版 pattern） -->
  <appender name="FILE_INFO" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/${APP_NAME}.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/${APP_NAME}-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>50MB</maxFileSize>
      <maxHistory>30</maxHistory>
      <cleanHistoryOnStart>true</cleanHistoryOnStart>
    </rollingPolicy>
    <encoder><pattern>${LOG_PATTERN_INFO}</pattern></encoder>
  </appender>

  <!-- DEBUG 詳細檔（只收 DEBUG 等級，含 MDC） -->
  <appender name="FILE_DEBUG" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/${APP_NAME}-debug.log</file>
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <level>DEBUG</level>
      <onMatch>ACCEPT</onMatch>
      <onMismatch>DENY</onMismatch>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/${APP_NAME}-debug-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>50MB</maxFileSize>
      <maxHistory>14</maxHistory>
    </rollingPolicy>
    <encoder><pattern>${LOG_PATTERN_DEBUG_MDC}</pattern></encoder>
  </appender>

  <!-- 全域錯誤檔（ERROR 以上，精簡版 pattern） -->
  <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/${APP_NAME}-error.log</file>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>ERROR</level>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/${APP_NAME}-error-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>20MB</maxFileSize>
      <maxHistory>60</maxHistory>
    </rollingPolicy>
    <encoder><pattern>${LOG_PATTERN_INFO}</pattern></encoder>
  </appender>

  <!-- 非同步包裝 -->
  <appender name="ASYNC_FILE_INFO" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="FILE_INFO"/>
    <queueSize>8192</queueSize>
    <neverBlock>true</neverBlock>
  </appender>
  <appender name="ASYNC_FILE_DEBUG" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="FILE_DEBUG"/>
    <queueSize>4096</queueSize>
    <neverBlock>true</neverBlock>
  </appender>
  <appender name="ASYNC_ERROR" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="ERROR_FILE"/>
    <queueSize>4096</queueSize>
    <neverBlock>true</neverBlock>
  </appender>

  <!-- Root：Console + 全域錯誤 -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ASYNC_ERROR"/>
  </root>

  <!-- 你的主專案包名（請依你的實際 base package；先用 com.EEITG3） -->
  <logger name="com.EEITG3" level="INFO" additivity="false">
    <!-- INFO 以上：寫到 Console + 一般檔案 + 錯誤檔 -->
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ASYNC_FILE_INFO"/>
    <appender-ref ref="ASYNC_ERROR"/>
    <!-- DEBUG 明細（含 rid/uid）另外寫到 debug 檔 -->
    <appender-ref ref="ASYNC_FILE_DEBUG"/>
  </logger>

  <!-- 降噪 -->
  <logger name="org.springframework" level="INFO"/>
  <logger name="org.hibernate.SQL" level="WARN"/>
  <logger name="org.apache.catalina" level="INFO"/>
</configuration>